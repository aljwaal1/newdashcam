name: Build Bike Adventure Android 4.4

on:
  workflow_dispatch:
  push:
    branches: [android-4.4]
    paths:
      - 'bike-kids-android-4.4/**'
      - '.github/workflows/build-bike-kids-android-4.4.yml'

permissions:
  contents: write

concurrency:
  group: bike-kids-android-4-4
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: android-4.4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Prepare Android SDK
        shell: bash
        run: |
          set -euo pipefail
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-33' 'build-tools;33.0.2' 'platform-tools'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '7.6.4'
      - name: Build signed debug APK
        shell: bash
        run: |
          set -o pipefail
          gradle -p bike-kids-android-4.4 clean assembleDebug --stacktrace --info --no-daemon 2>&1 | tee bike-kids-build.log
      - name: Save and verify APK
        shell: bash
        run: |
          set -euo pipefail
          APK="$(find bike-kids-android-4.4/app/build/outputs/apk -type f -name '*debug*.apk' | head -n 1)"
          test -n "$APK"
          test -s "$APK"
          unzip -t "$APK" >/dev/null
          "$ANDROID_HOME/build-tools/33.0.2/apksigner" verify --verbose --print-certs "$APK"
          mkdir -p apk
          cp "$APK" apk/bike-adventure-android-4.4.apk
          sha256sum apk/bike-adventure-android-4.4.apk
      - uses: actions/upload-artifact@v4
        with:
          name: bike-adventure-android-4.4
          path: apk/bike-adventure-android-4.4.apk
          if-no-files-found: error
      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: bike-adventure-build-log
          path: bike-kids-build.log
          if-no-files-found: ignore
      - name: Commit APK
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add apk/bike-adventure-android-4.4.apk
          if ! git diff --cached --quiet; then
            git commit -m 'Build Bike Adventure Android 4.4 APK'
            git pull --rebase origin android-4.4
            git push origin HEAD:android-4.4
          fi
